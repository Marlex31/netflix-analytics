
plotting:
    add time conversion
    add watching bar plot
    find a way to plot genre preference

remove seconds from scrape.duration

rewrite imdb_scrape func by removing ret statements

add support for not_found items from list_3

can replace test comparing title to string with result.media in some cases

use stdout flush or sth do display scraping progress

SAO movie does't get duration value